import { Pressable, View } from 'react-native';
import CustomIcon from '../UI/CustomIcon';

export default function LinkedAccountsOtherBlock() {
	return (
		<View>
			<Pressable>
				<CustomIcon svg={''} />
			</Pressable>
		</View>
	);
}
